# Integration tests

Testes de integração devem residir aqui (API ou DB). O backend legado MySQL (`backend/` na raiz) foi removido com sua suíte Jest associada.

Smoke rápido da API atual (Postgres/Prisma): use `npm run test:smoke` na raiz.
