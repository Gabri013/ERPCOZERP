# Copie para `apps/backend/.env` ou defina na raiz do monorepo (`../../.env`).
# Opcional: se DATABASE_URL estiver ausente e NODE_ENV não for production,
# o script `sync:cozinca-team` monta a URL a partir dos defaults do docker-compose.

# DATABASE_URL=postgresql://erpcoz:erpcozpass@127.0.0.1:5432/erpcoz
