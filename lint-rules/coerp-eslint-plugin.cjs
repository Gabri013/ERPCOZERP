/**
 * COERP Custom ESLint Plugin
 * 
 * Define regras customizadas para o projeto:
 * - Validação de convenções de projeto
 * - Enforce de padrões de código Cozinka
 * - Regras de performance e segurança
 */

module.exports = {
  rules: {
    // Adicione regras customizadas aqui quando necessário
  },
  configs: {
    recommended: {
      rules: {
        // Regras recomendadas para o projeto
      },
    },
  },
};
