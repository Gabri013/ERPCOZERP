/**
 * Ponto único pedido pela convenção `/lint-rules/custom-rules.js`.
 * Re-exporta o plugin COZINCA (CommonJS para compatível com ESLint + require dinâmico).
 */
module.exports = require('./coerp-eslint-plugin.cjs');
